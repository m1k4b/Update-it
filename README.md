# Update-it
Update It is a software that allows you to easily install Minecraft mods.


# How does it work?
You can create modpacks and add mods to them. Then, you can easily download them for a version of Minecraft.


# How to install?
Run `pip install -r requirements.txt`, then run the program console_app.py, which needs to be in the same directory as tools.py.
